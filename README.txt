# ATTENTION：将目录下的Qt_UDP_DTS目录置于C盘根目录下

# Qt_UDP_DTS_v2.0.0
为应用在飞机上的分布式温度传感器开发的上位机软件
1、增加了最大值法确定中心波长、质心法确定中心波长
2、增加了相关的波形显示

# Qt_UDP_DTS_v2.1.0
1、增加了温度判断算法及波形显示

# Qt_UDP_DTS_v3.0.0
1、调整了波形显示界面，让五个波形都显示在主界面

# Qt_UDP_DTS_v3.1.0
1、使用echarts+html+js和Qt联调成功，波形显示功能更强大，图像更美观

# Qt_UDP_DTS_v3.2.0
1、增加了UDP通信功能，接收FPGA发送的HEX数据

# Qt_UDP_DTS_v3.3.0
1、修改了前端界面，增加了最高温度显示仪表盘，光纤段最高/低温度距离点标注，过热报警LCD时间计时功能

# Qt_UDP_DTS_v3.4.0
1、修改了前端view的显示，只使用一个view展示html网页
2、增加了config.ini配置文件，任何参数的修改在配置文件里操作

# Qt_UDP_DTS_v3.6.0
1、对前端界面进行了美化
2、视频录制的版本

# Qt_UDP_DTS_v3.6.1
1、修改温度报警逻辑功能

#Qt_UDP_DTS_v3.6.2
1、增加历史温度曲线弹窗功能